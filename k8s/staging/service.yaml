apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: staging
  labels:
    app: appointment-service
    env: staging
spec:
  type: ClusterIP
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
